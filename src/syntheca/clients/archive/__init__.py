"""Archived/deprecated client implementations.

This directory contains client code that has been superseded by newer
implementations. Files here are kept for historical reference only and
must NOT be imported at runtime.

- ``pure_oai_lxml.py`` — lxml-based Pure OAI-PMH parser, replaced by
  the xmltodict-based ``pure_oai.py`` in the parent package.
"""
